export enum EStatus {
    INACTIVE = "inactive",
    ACTIVE = "active"
}

export enum EUserStatus {
    INACTIVE = "inactive",
    ACTIVE = "active",
    PENDING = "pending"
}

export enum EContactStatus {
    PENDING = "pending",
    RESOLVED = "resolved",
}