export enum ETheme {
  LIGHT = "light",
  DARK = "dark",
}

export enum EStatus {
    INACTIVE = "inactive",
    ACTIVE = "active"
}

export enum EUserStatus {
    PENDING = "pending"
}

export enum EContactStatus {
    PENDING = "pending",
    RESOLVED = "resolved",
}